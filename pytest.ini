[pytest]
testpaths = tests loraflexsim/tests
markers =
    slow: tests that take longer to run
    integration: end-to-end or integration scenarios
    propagation_campaign: campagne propagation (pertes de parcours, portée, PER)
